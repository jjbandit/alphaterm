class InterfaceConstants {

  constructor () {
    this.FOCUS_COMMAND_LINE = 'focus_command_line';
    this.REDIRECT           = 'redirect';
  }
}

let x = new InterfaceConstants();
export default x;
